services:
  lb:
    hostname: lb
    image: ubuntu:rust-docker
    ports:
      - 6170:6170 
    working_dir: /rust
    environment:
      - RUSTFLAGS=-C lto=true 
    tty: true
    volumes:
      - ../:/rust
      -  /var/run/docker.sock:/var/run/docker.sock